import { AniDirective } from './ani.directive';

describe('AniDirective', () => {
  it('should create an instance', () => {
    const directive = new AniDirective();
    expect(directive).toBeTruthy();
  });
});
