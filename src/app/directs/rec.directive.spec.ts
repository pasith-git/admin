import { RecDirective } from './rec.directive';

describe('RecDirective', () => {
  it('should create an instance', () => {
    const directive = new RecDirective();
    expect(directive).toBeTruthy();
  });
});
