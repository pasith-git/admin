import { NavEventDirective } from './nav-event.directive';

describe('NavEventDirective', () => {
  it('should create an instance', () => {
    const directive = new NavEventDirective();
    expect(directive).toBeTruthy();
  });
});
